plugins {
    id 'com.android.library'
    id 'org.jetbrains.kotlin.android'
}

android {
    namespace 'com.kaleksandra.coretheme'
}

dependencies {
    implementation(core)
    implementation(composeUi)
    implementation(composePreview)

    implementation(material3)
}